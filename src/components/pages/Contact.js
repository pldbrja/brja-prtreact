import React from 'react';
import ContactForm from './ContactForm';

const styles = {
    Test: {
        opacity: 0,
    }
}

export default function Contact () {
    return (
        <div>
            <ContactForm />
        </div>
    );
};